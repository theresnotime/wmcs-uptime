name: Makefile CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  makeci:
    name: makeci
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: make ci-no-git
      run: make ci-no-git
